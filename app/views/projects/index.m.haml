#page
  #projects
    - for project in @projects
      .project
        = link_to h(project), project_path(project)
    - if current_user.can_create_project?
      .project
        = link_to t('.new_project'), new_project_path, :class => 'new'
